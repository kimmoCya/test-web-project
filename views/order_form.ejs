<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>주문 및 결제하기</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Apple SD Gothic Neo", "Noto Sans KR", system-ui, sans-serif; padding: 40px; background-color: #f4f6fa; color: #334155; }
        .order-container { max-width: 600px; margin: 0 auto; background: white; padding: 35px; border-radius: 12px; border: 1px solid #e2e8f0; box-shadow: 0 4px 6px rgba(0,0,0,0.03); }
        h2 { font-size: 20px; font-weight: 700; margin-top: 0; margin-bottom: 25px; border-left: 5px solid #2563eb; padding-left: 12px; color: #1e293b; }
        .section-title { font-size: 14px; font-weight: 600; color: #64748b; text-transform: uppercase; margin-bottom: 10px; border-bottom: 1px solid #e2e8f0; padding-bottom: 5px; }
        .user-info-box { background-color: #f8fafc; padding: 15px; border-radius: 8px; margin-bottom: 25px; font-size: 14px; line-height: 1.6; }
        .item-list { list-style: none; padding: 0; margin: 0 0 25px 0; }
        .item-row { display: flex; justify-content: space-between; align-items: center; padding: 12px 0; border-bottom: 1px dashed #e2e8f0; font-size: 14px; }
        .price-summary { display: flex; justify-content: space-between; align-items: center; background-color: #eff6ff; padding: 18px; border-radius: 8px; margin-bottom: 30px; }
        .total-label { font-size: 15px; font-weight: bold; color: #1e40af; }
        .total-amount { font-size: 20px; font-weight: 800; color: #2563eb; }
        .btn-checkout { background-color: #2563eb; color: white; border: none; padding: 14px; font-size: 15px; font-weight: bold; border-radius: 6px; cursor: pointer; width: 100%; box-shadow: 0 4px 6px rgba(37, 99, 235, 0.15); transition: background 0.2s; }
        .btn-checkout:hover { background-color: #1d4ed8; }
        .btn-cancel { display: block; text-align: center; margin-top: 15px; color: #94a3b8; text-decoration: none; font-size: 13px; }
        .btn-cancel:hover { color: #64748b; text-decoration: underline; }
    </style>
</head>
<body>

<div class="order-container">
    <h2>💳 주문서 작성 / 안전 결제</h2>

    <div class="section-title">주문 고객 정보</div>
    <div class="user-info-box">
        <strong>이름:</strong> <%= user.name %> (<%= user.username %>)<br>
        <strong>연락처:</strong> <%= user.phone || '등록된 번호 없음' %><br>
        <strong>배송지 주소:</strong> <%= user.address || '마이페이지에서 주소를 설정해 주세요.' %>
    </div>

    <div class="section-title">결제 대상 상품 내역</div>
    <ul class="item-list">
        <% orderItems.forEach(item => { %>
            <li class="item-row">
                <div>
                    <span style="font-size: 18px; margin-right: 4px;"><%= item.emoji %></span>
                    <strong><%= item.name %></strong>
                    <span style="color: #94a3b8; font-size: 12px;">(x<%= item.quantity %>개)</span>
                </div>
                <span style="font-weight: 500;">₩<%= item.total.toLocaleString() %></span>
            </li>
        <% }) %>
    </ul>

    <div class="price-summary">
        <span class="total-label">최종 결제 금액</span>
        <span class="total-amount">₩<%= totalPrice.toLocaleString() %></span>
    </div>

    <form action="/order/checkout" method="post" onsubmit="return confirm('최종 금액을 확인하였으며 결제를 승인하시겠습니까?')">
        <button type="submit" class="btn-checkout">🔒 ₩<%= totalPrice.toLocaleString() %> 안전 결제하기</button>
    </form>

    <a href="/cart" class="btn-cancel">결제 취소하고 장바구니로 돌아가기</a>
</div>

</body>
</html>